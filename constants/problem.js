const Types = {
    MC: "MC",
};

/**
 * @type {{ Base: Number }} - Number of alternatives for a basic question.
 */
const Alternatives = {
    Base: 5
};

const ButtonTypes = {
    GET_HINT: "getHint"
};

module.exports = {
    Types, Alternatives, ButtonTypes
};
