// Declare global variables.
const http = require('http');

module.exports = {
    name: 'mathconstructor',
    description: 'Math Constructor',
    run: async (problemReq) => {
        // problemReq [difficulty, type, category, lang, ]
    }
}